<h1>Feed de Oportunidades</h1>
<form method="get">
  <input name="q" value="{{q}}" placeholder="Buscar..." />
  <button type="submit">Buscar</button>
</form>
<ul>
  {{#each opportunities}}
    <li><a href="/volunteer/opportunities/{{this.id}}">{{this.title}} — {{this.location}}</a></li>
  {{/each}}
</ul>
